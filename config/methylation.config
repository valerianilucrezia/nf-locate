process {
    withName: 'MODKIT' {
        cpus= 12
        memory='100 GB'
        time='48h'

        publishDir = [
            [
                mode: 'copy',
                path: { "${params.outdir}/methylation/modkit/" },
                pattern: "*{bed, log}"
            ]
        ]
    }

    withName: 'GET_POSITIONS' {
        memory='20 GB'

        publishDir = [
            [
                mode: 'copy',
                path: { "${params.outdir}/methylation/get_positions/" },
                pattern: "*{_meth}"
            ]
        ]
    }
    
    withName: 'READ_BED' {
        memory='16 GB'

        publishDir = [
            [
                mode: 'copy',
                path: { "${params.outdir}/methylation/read_bed/" },
                pattern: "*{RDS}"
            ]
        ]
    }

    withName: 'METH_PILEUP' {
        memory='100 GB'
        cpus=12
        time='24h'

        publishDir = [
            [
                mode: 'copy',
                path: { "${params.outdir}/methylation/pileup/" },
                pattern: "*{vcf}"
            ]
        ]
    }

    withName: 'COMBINE' {
        memory='50 GB'

        publishDir = [
            [
                mode: 'copy',
                path: { "${params.outdir}/methylation/combine/" },
                pattern: "*{RDS}"
            ]
        ]
    }


}
