process {
    withName: 'SPLIT_ALIGN' {
        publishDir = [
            [
                mode: params.publish_dir_mode,
                path: { "${params.outdir}/split/${meta.sampleID}/${meta.type}/" },
                pattern: "*{bam,bai,cram,crai}"
            ]
        ]
    }
}